rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /requests/{document=**} {
      allow read;
      allow write: if isAdmin();
    }

    function isAdmin() {
      return request.auth.uid == "PEYNjKn9QDNcmNSmiEY8ox6R1CP2";
    }
  }
}